import AuthForm from "@/components/AuthForm";

const SignUp = () => {
    return (
        <section className="flex-center sizze-full max-sm:px-6">
            <AuthForm type="sign-up" />
        </section>
    );
};

export default SignUp;
